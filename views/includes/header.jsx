import React from 'react';
import ReactDOM from 'react-dom';

export default class Header extends React.Component {
  render() {
    return (
        <header>
            <div>
                Tutorial with React, React-Dom, Redux, React-Router and more
            </div>
        </header>
        );
    }
}